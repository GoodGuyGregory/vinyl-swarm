[package]
name = "bookstore"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
futures = "0.3.31"
sqlx = { version = "0.8.3", features = ["postgres", "runtime-tokio"] }
tokio = {version = "1.0", features = ["full"]}
uuid = "1.15.1"

